# Timeline 修复与验证（2026-09-24）

环境：D:/Hodgepodge，UE 5.5.4，Win64 Development。MCP `bridge_status` 确认项目身份后执行。

## 修改

- `UHodgeAbilityTimeline::ValidateForPlayback` 提供编辑期与运行期共用的校验；保留编辑器提示，避免两份安全规则漂移。
- Window GE 只允许 Infinite 且 StackingType=None。堆叠 GE 的句柄可能被多个来源共享，不能用窗口独占句柄的方式回收。
- Point GE 只允许 Instant/HasDuration，并保留对 LocallyControlledOnly + GE 的禁止。
- 校验时间范围、有限数值、枚举、标签、EventID 和同标签窗口重叠；Task 在执行任何条目前校验，失败只记录错误并结束 Task。
- 增加 `Hodge.Timeline` 三项 Automation Tests 和 `Tools/TimelineTests/verify_pie.py`。
- Game 构建发现已有 `HodgeEnemyCharacter::PostEditChangeProperty` 实现缺少 WITH_EDITOR 保护；仅补对应保护，保留原文件 UTF-16 编码。

未修改、重存 Timeline 数据资产或 GA 测试蓝图；保留工作区已有改动，未提交。

## 构建命令及结果

编辑器无未保存资产和地图后，通过 MCP `unreal.SystemLibrary.quit_editor()` 正常关闭。

```powershell
& 'D:/UE_5.5/Engine/Build/BatchFiles/Build.bat' HodgepodgeEditor Win64 Development '-Project=D:/Hodgepodge/Hodgepodge.uproject' -WaitMutex -architecture=x64
& 'D:/UE_5.5/Engine/Build/BatchFiles/Build.bat' Hodgepodge Win64 Development '-Project=D:/Hodgepodge/Hodgepodge.uproject' -WaitMutex -architecture=x64
```

最终两个目标退出码均为 0。Game 首次因上述编辑器函数缺少保护失败，补齐后通过。工具链实际使用 MSVC 14.38.33145、Windows SDK 10.0.22621.0。未升级构建配置、引擎或插件。已有插件依赖、IncludeOrder、WaitMoveCancel 内联生成代码及 HUD 弃用警告未扩展处理。

## MCP 自动化测试

重开编辑器后，通过 `system_control.run_tests`，filter=`Hodge.Timeline`，执行 `automation RunTests Hodge.Timeline`。

- `Hodge.Timeline.Validation`：通过。覆盖窗口堆叠/时长策略、Point 时长策略及非有限时间。
- `Hodge.Timeline.RejectBeforeSideEffects`：通过。非法窗口不能改变已有 GE 层数；非法 Infinite Point 不派发事件、不残留 GE。
- `Hodge.Timeline.CleanupAndOrdering`：通过。重叠窗口独立回收、外部 GE 保留、边界 Point 看到正确状态、自然完成、中断、重复停止和 Owner 结束清理。

日志在 02:34:57–02:34:58 UTC 记录三项 Test Completed 成功。首次测试因夹具重复初始化 World 导致编辑器崩溃；修正为仅由 CreateWorld 初始化一次，重新构建后上述测试通过。该次崩溃发生在测试初始化阶段。

## 蓝图与 PIE

MCP `manage_blueprint.compile` 检查 `/Game/CodexText/Timeline/GA_TimelineTest`：compiled=true、compilerStatus=UpToDate、saved=false。

在 `/Game/CodexText/Locomotion/L_LocomotionLab` 单人 PIE/Simulate 中，向实际 HodgePlayerState ASC 临时授予 GA_TimelineTest，九项断言全部通过：

- 约 0.10 秒：Windup=1，其余窗口为 0，无窗口 GE。
- 约 0.30 秒：Active=1，窗口 GE 数量增加 1。
- 约 0.70 秒：Recovery=1、MoveCancel=1，Active GE 已回收。
- 约 1.15 秒：全部窗口 Tag 和 GE 清零。
- 再次激活，在 Active 窗口中通过 `clear_ability` 移除本次临时授予的能力：Tag 与 GE 立即清零。

采样结果保存在 `Saved/Tests/timeline_pie_result.json`。运行中的能力移除测试使用真实 ASC 清理入口；不能通过 GetGameplayAbilityFromSpecHandle 返回的 CDO 调用 EndAbility 来验证 InstancedPerExecution 能力。初版脚本因此产生的错误断言已纠正。

复测：打开上述地图并启动单人 PIE，然后通过 MCP `system_control.execute_python`，file=`Tools/TimelineTests/verify_pie.py`。约两秒后查看结果 JSON 的 passed=true；结束 PIE。脚本只清理由自己授予的能力，不保存资产。

## 验证边界

这次验证了 C++ Editor/Game 构建、测试蓝图编译、三项本地自动化测试及单人 PIE 中的 Timeline 状态清理。未执行双端网络、预测回滚、蓝图自然结束自身、Cook 或打包验证。Task 不负责结束 GA 的原有契约保持不变；调用方仍需自行处理 Timeline.End。

最终检查：`git diff --check` 针对本次文本改动通过；原有资产与源码改动保留，未暂存或提交。测试生成的日志、缓存与 JSON 不纳入源码变更。
