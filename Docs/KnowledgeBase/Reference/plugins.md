# 本地插件描述索引

[知识库首页](../README.md) · [参考入口](README.md)

> 自动生成的静态导航；行为结论以人工章节和源码为准。

仅提取本地插件描述和 uproject 显式选择，不确认实际加载、连接或编译。业务源码索引不包含这些插件实现。

## ALS

[插件描述](../../../Plugins/ALS-Refactored-4.15/ALS-Refactored-4.15/ALS.uplugin)

- 版本：4.15
- EngineVersion：5.5.0
- EnabledByDefault：True
- uproject 显式 Enabled：未声明
- 模块：ALS (Runtime)；ALSCamera (Runtime)；ALSExtras (Runtime)；ALSEditor (UncookedOnly)
- 插件依赖：ACLPlugin (Enabled=True)；AnimationModifierLibrary (Enabled=True)；ControlRig (Enabled=True)；EngineCameras (Enabled=True)；EnhancedInput (Enabled=True)；GameplayTagsEditor (Enabled=True)；Metasound (Enabled=True)；Niagara (Enabled=True)；PropertyAccessNode (Enabled=True)

## RiderLink

[插件描述](../../../Plugins/Developer/RiderLink/RiderLink.uplugin)

- 版本：2025.2.2.1
- EngineVersion：未声明
- EnabledByDefault：True
- uproject 显式 Enabled：未声明
- 模块：RD (EditorNoCommandlet)；RiderLink (EditorNoCommandlet)；RiderLogging (EditorNoCommandlet)；RiderBlueprint (EditorNoCommandlet)；RiderGameControl (EditorNoCommandlet)；RiderShaderInfo (EditorNoCommandlet)；RiderLC (EditorNoCommandlet)；RiderDebuggerSupport (EditorNoCommandlet)
- 插件依赖：

## UnrealMCP

[插件描述](../../../Plugins/UnrealMCP/UnrealMCP.uplugin)

- 版本：1.0
- EngineVersion：未声明
- EnabledByDefault：True
- uproject 显式 Enabled：未声明
- 模块：UnrealMCP (Editor)
- 插件依赖：EditorScriptingUtilities (Enabled=True)；Niagara (Enabled=True)；PythonScriptPlugin (Enabled=True)；Metasound (Enabled=True)
