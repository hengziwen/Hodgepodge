# 2026-09-13 知识库更新记录

[返回首页](README.md) · [当前接通清单](12-integration-backlog.md)

## 核对基线

本轮先读 AGENTS.md 与 Docs/AI_DEVELOPMENT.md，再对旧快照检查，发现 46 个源码、配置和 Main 资产变化。开始时 HEAD 为 `8fe5ac8`，工作区已有 HeroComponent.cpp 与 GameFeatureAction_AddInputContextMapping.cpp 的诊断修改，全部保留。

开发流程文件包含历史现场记录，例如 PawnData 只有 PawnClass 的段落已落后于当前源码。规则仍遵循，事实以当前文件为准。本轮不改该历史记录文件，不把旧编译或编辑器日志当作新验证。

## 已确认的新入口

- GameMode 使用 HodgePlayerController，并在 FinishSpawning 前注入 PawnData。
- HeroComponent 已启用，HeroCharacter 构造挂载；初始化、输入、相机模式选择均有有效代码。
- 控制器 PostProcessInput 消费技能输入，构造选择 HodgePlayerCameraManager。
- PawnExtension 设置 TagRelationshipMapping，解除分支清 AbilityInput。
- HodgeAbilitySystemGlobals 分配 Hodge Context，Globals/CueManager/LocalPlayer/Policy/InputComponent 均有项目配置。
- HealthSet 新增 Damage/Healing 元属性及 GE 后结算、耗尽通知。
- GameFeature 输入添加入口恢复；Cue 观察者添加路径函数恢复，但观察者本身未注册。

## 剩余重点

AbilitySet 基础授予仍停用；Hero IMC 添加受 RegisterWithSettings 条件控制；额外输入移除为空，句柄没有持久化；Hero 旧直接初始化尚在；控制器与扩展组件清 Avatar 顺序待验证；HealthComponent、敌人 ASC、完整命中死亡、专服启动尚有缺口。完整说明在 KB 清单，避免把旧编号误当成全部待从零实现。

## 新资产与插件

新增 `/Game/Main/Camera/CM_Default`、`/Game/Main/Character/Hero/BP_HeroBase`、`/Game/Main/Character/Hero/BP_Hero_Pover`。PawnData、InputConfig 和两份动画资产内容哈希变化，未解析内部值。

UNTLink 显式禁用；ALS 描述默认启用；发现 UnrealMCP 1.0 Editor 插件描述及依赖。插件描述加入快照跟踪，插件实现不纳入业务源码索引。

## 工具维护

部分源码使用 UTF-16 BOM，读取工具现识别 UTF-16，避免索引出现 NUL/乱码。快照范围新增 AGENTS.md、Docs/AI_DEVELOPMENT.md 和所有本地 uplugin 描述。旧快照没有这些文件不等于本轮新增了它们；它们只是新纳入跟踪。

更新了人工章节和 PURPOSE 类职责，再刷新 Reference；不是只刷新快照掩盖旧结论。本轮仅修改知识库文档/工具，不修改业务代码、配置、资产或插件。

## 验证边界

执行 `python Docs/KnowledgeBase/tools/kb.py refresh`、`check` 和全文搜索；用 Git 文档差异检查与哈希比对确认没有覆盖游戏文件。具体扫描数量见快照。

UE Editor/Game 构建、蓝图 Compile、PIE、联机和打包均未执行。所有新入口只认定源码/配置已接入，实际可玩仍按验收手册验证。
